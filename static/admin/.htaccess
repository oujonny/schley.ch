# Allow the CMS config file to be served
# (LiteSpeed/Apache on cyon blocks .yml by default)
<FilesMatch "\.ya?ml$">
    Require all granted
</FilesMatch>

# Prevent COOP from severing the opener reference when the OAuth popup returns
Header always set Cross-Origin-Opener-Policy "unsafe-none"
